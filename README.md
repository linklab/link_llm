# link_lmm

언어 모델(LMM)을 From Scratch 로, 버전별로 관리하고 웹앱에서 테스트하는 프로젝트입니다.

## 폴더 구조

```
link_lmm/
├── lmm/                # 모델 버전들을 모아두는 곳
│   ├── v0.0.1/         #   - 앞 단어 1개로 예측 (가장 기본)
│   │   ├── base.py     #      NGramLM: 이 버전이 '처음 필요로 한' 함수들 (기반 클래스)
│   │   ├── model.py    #      이 버전 설정(ORDERS)만 선언
│   │   ├── data/       #      data.txt (학습용 문장)
│   │   ├── train/      #      얇은 train.py
│   │   ├── models/     #      model.json (학습 결과)
│   │   └── test/       #      얇은 test.py
│   ├── v0.0.2/         #   - 앞 단어 2개 (base.py 는 v0.0.1 을 상속, 새 코드 없음)
│   ├── v0.0.3/         #   - 문장 끝(<END>) 학습    (base.py 가 <END> 추가)
│   ├── v0.0.4/         #   - 백오프(2→1단어 재시도)  (base.py 가 백오프 추가)
│   ├── v0.0.5/         #   - 개수→확률 + 온도        (base.py 가 온도 추가)
│   ├── v0.0.6/         #   - 문장부호 토크나이저      (base.py 가 토크나이저 교체)
│   └── v0.0.7/         #   - top-k / top-p 샘플링     (base.py 가 choose 자르기 추가)
├── web_service/        # ChatGPT 스타일 웹앱 (선택한 버전의 base.py 를 불러와 재사용)
│   ├── server.py
│   └── index.html
└── README.md
```

## 코드 구조 (버전별 base.py + 상속 사슬)

버전마다 코드가 거의 똑같이 반복되던 것을, **상속 사슬**로 정리했어요.
각 버전의 `base.py` 는 **그 버전이 처음 추가한 함수만** 담고, 나머지는 이전 버전에서 물려받아요.

```
v0.0.1/base.py  NGramLM (기반: 읽기·학습·저장·생성 전부)
      ▲ 상속
v0.0.2/base.py  (새 코드 없음 — ORDERS=[2] 설정만으로 앞 2단어)
      ▲
v0.0.3/base.py  + prepare()/is_end()      ← 문장 끝 <END>
      ▲
v0.0.4/base.py  + next_token()/can_continue()  ← 백오프
      ▲
v0.0.5/base.py  + choose()                ← 확률 + 온도
      ▲
v0.0.6/base.py  + tokenize()/detokenize() ← 문장부호 분리
      ▲
v0.0.7/base.py  + choose()/generate()     ← top-k / top-p 샘플링
```

- **각 버전 `base.py`** — 그 버전이 **새로 추가/변경한 함수**만. → "이 버전이 뭘 더했나"가 한눈에.
- **각 버전 `model.py`** — 설정 `ORDERS` (문맥 표 길이)만 선언.
- **`train/train.py`·`test/test.py`** — `model.py` 를 불러와 실행하는 **얇은 껍데기**.
- **`web_service/server.py`** — 선택된 버전의 `base.py` 를 불러와 `generate` 만 호출.

> 폴더 이름에 `.` 이 있어 일반 `import` 가 안 되므로, 이전 버전 `base.py` 는 파일 경로로
> 불러와요(각 `base.py` 상단의 `_inherit`). "이 버전이 뭘 바꿨나"는 그 `base.py` 하나만,
> "전체가 어떻게 도나"는 `v0.0.1/base.py` 부터 사슬을 따라 읽으면 됩니다.
> 모델 저장 형식은 모든 버전이 `{ "max_order", "tokenizer", "tables" }` 로 통일돼요.

## 버전 목록

- [**v0.0.1**](lmm/v0.0.1/) — "단어 세기"만으로 문장을 만드는 아주 쉬운 언어 모델
  (앞 단어 1개로 예측). 자세한 설명은 [lmm/v0.0.1/README.md](lmm/v0.0.1/README.md) 참고.
- [**v0.0.2**](lmm/v0.0.2/) — 앞 단어 **2개**로 예측해 문장이 더 자연스러워졌고,
  학습 데이터를 더 다양하고 많게 늘렸어요. 자세한 설명은 [lmm/v0.0.2/README.md](lmm/v0.0.2/README.md) 참고.
- [**v0.0.3**](lmm/v0.0.3/) — 문장 끝(`<END>`)을 학습해, 정해진 길이에서 툭 끊기지 않고
  **자연스럽게 마무리**돼요. 자세한 설명은 [lmm/v0.0.3/README.md](lmm/v0.0.3/README.md) 참고.
- [**v0.0.4**](lmm/v0.0.4/) — **백오프**: 앞 2단어 문맥이 없으면 1단어로 줄여 다시 찾아요.
  덕분에 **1단어만 입력해도** 그 단어로 시작하는 문장을 만들 수 있어요.
  자세한 설명은 [lmm/v0.0.4/README.md](lmm/v0.0.4/README.md) 참고.
- [**v0.0.5**](lmm/v0.0.5/) — 개수를 **확률**로 바꾸고 **온도(temperature)** 로 다양성을 조절해요
  (실제 GPT의 문장 생성 방식). 웹앱에 온도 슬라이더가 있어요.
  자세한 설명은 [lmm/v0.0.5/README.md](lmm/v0.0.5/README.md) 참고.
- [**v0.0.6**](lmm/v0.0.6/) — **토크나이저 개선**: 문장부호(. , ! ?)를 따로 떼어 학습해
  `"좋아"` 와 `"좋아!"` 를 같은 단어로 다루고, 문장부호가 언제 나오는지도 배워요.
  자세한 설명은 [lmm/v0.0.6/README.md](lmm/v0.0.6/README.md) 참고.
- [**v0.0.7**](lmm/v0.0.7/) — **top-k / top-p 샘플링**: 뽑기 전에 확률 낮은 '꼬리 후보'를
  잘라내 안정성을 높여요. 확률 분포에만 작용해 나중에 신경망에서도 그대로 재사용됩니다.
  자세한 설명은 [lmm/v0.0.7/README.md](lmm/v0.0.7/README.md) 참고.

## 웹앱 실행

```bash
python3 web_service/server.py
```

브라우저에서 접속:

```
http://127.0.0.1:8000
```

우측 상단에서 **모델 버전**을 고르고 메시지를 보내면, 그 버전이 학습한 내용을
바탕으로 대답을 만들어 줘요. `lmm/` 안에 새 버전 폴더를 만들면 자동으로 목록에 추가됩니다.

## 모르는 단어가 입력되면?

이 모델은 입력 문장의 **마지막 단어**를 시작점으로 문장을 만들어요.

- 마지막 단어가 모델에 **있으면** → 그 단어부터 시작
- 마지막 단어가 모델에 **없으면**(모르는 단어) → 모델이 아는 단어 중 **무작위로 하나**를 골라 시작

즉, 모르는 단어가 들어와도 오류 없이 항상 문장을 만들어 냅니다(폴백 방식).
대신 입력과 상관없는 엉뚱한 답이 나올 수 있는데, 이는 단어의 *의미*를 이해하는 게 아니라
"어떤 단어 뒤에 무슨 단어가 나왔는지"만 세어둔 아주 단순한 모델이기 때문이에요.

## v0.0.2·v0.0.3 에서 단어를 1개만 입력하면?

v0.0.2·v0.0.3 은 **앞 두 단어**로 다음 단어를 찾는 모델이에요. 그래서 입력이 1개뿐이면
두 단어짜리 문맥을 만들 수 없어, 다음 순서로 처리됩니다.

1. 마지막 두 단어가 필요한데 1개뿐 → 문맥을 못 만듦(빈 문맥)
2. 빈 문맥은 표에 없으므로 → **폴백**: 모델이 아는 두 단어 문맥 중 **무작위로 하나**를 골라 시작
3. 그 문맥부터 단어를 이어 붙여 문장을 완성 (v0.0.3 은 `<END>` 에서 자연스럽게 멈춤)

즉, **입력한 한 단어는 결과에 나오지 않고 무시**되며(무작위 시작), 시작 방식은 두 버전이 같고
**끝맺음만** 다릅니다(v0.0.2 는 툭 끊길 수 있고, v0.0.3 은 자연스럽게 마무리).

> ✅ **v0.0.4 부터 해결됨**: v0.0.4 의 **백오프**(앞 2단어가 없으면 1단어로 줄여 재시도) 덕분에,
> `"나는"` 한 단어만 입력해도 `"나는"` 으로 시작하는 문장을 만들 수 있어요. 더 이상 입력이 무시되지 않아요.

### 백오프 전후 비교 (1단어 `"나는"` 입력)

| 버전 | 1단어 "나는" 입력 결과 | 해석 |
|------|----------------------|------|
| v0.0.2 / v0.0.3 (백오프 X) | "단풍이 곱게 물듭니다" / "기분이 조금 나쁩니다" | 2단어 문맥 못 만들어 **입력 무시, 무작위 시작** |
| v0.0.4 / v0.0.5 / v0.0.6 (백오프 O) | "나는 아침에 밥을 먹습니다" 등 | 1단어로 줄여 찾아 **입력을 반영** ✅ |

## 새 버전을 만들려면?

`lmm/v0.0.2` 처럼 `lmm/` 아래에 새 폴더를 만들어 관리하면 이전 버전을 그대로 보존할 수 있어요.
(새 버전은 `train/train.py`를 한 번 실행해 `models/model.json`을 만들어야 웹앱 목록에 나타납니다.)
