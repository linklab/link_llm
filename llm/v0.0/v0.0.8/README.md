# link_llm — 나만의 작은 언어 모델 (v0.0.8)

누구나 쉽게 이해할 수 있는 **아주 기본적인** 언어 모델(LLM)이에요.

## v0.0.7 → v0.0.8, 무엇이 바뀌었나요?

> **대화 형식 + 멀티턴 문맥** — ChatGPT 같은 '대화 골격' 을 갖춰요.

### 1) 대화 형식 (역할 토큰)

누가 말하는 차례인지 **역할 토큰**으로 표시해요.

- `<사용자>` : 사용자 차례
- `<봇>` : 봇(모델) 차례

학습 데이터(`공용 data/data.txt`)도 한 줄이 한 번의 주고받기예요:

```
<사용자> 안녕 <봇> 안녕하세요!
<사용자> 고마워 <봇> 천만에요!
```

그래서 모델은 **"`<봇>` 다음엔 답이 오고 `<END>` 로 끝난다"** 를 배웁니다.
문장을 만들 때는 `<사용자> {내 말} <봇>` 까지 주고 그 뒤(봇의 답)를 이어 붙여요.

### 2) 멀티턴 문맥

이전 대화(기록)를 문맥으로 이어받아 다음 답을 만드는 것이에요.

```
<사용자> 안녕 <봇> 안녕하세요! <사용자> 이름이 뭐야? <봇> ___
```

지금까지의 대화를 통째로 문맥으로 삼아, `lm.chat(입력, 기록)` 이 **봇의 답만** 돌려줍니다.

## 역할 토큰(`<사용자>`/`<봇>`)은 꼭 필요할까?

모델 표의 열쇠(문맥)에 역할 토큰을 넣는 게 맞는지 자주 묻는 질문이에요. 나눠서 보면:

### `<봇>` — 진짜로 일하는 토큰 ✅ (경계/트리거)

`<봇>` 은 **"여기서부터 봇의 답을 만들어"** 라는 신호예요.

- 있으면: 문맥 `빌어 <봇>` → `고마워요` 처럼 **"텍스트 이어쓰기"를 "대답 생성"으로** 바꿔줘요.
- 없으면: 사용자 말과 답의 **경계가 흐려져요** (그냥 문장 이어붙이기가 됨 = v0.0.7).

### `<사용자>` — 우리 2-gram에선 거의 장식

답의 첫 단어를 정할 때 보는 문맥은 **마지막 2토큰**(`빌어 <봇>`)이라, `<사용자>` 는 거기에 안 들어가요.
`<사용자>` 가 실제로 쓸모 있어지는 곳은:

1. **멀티턴 구분** — `…함께하길! <사용자> 안녕 <봇>` 처럼 이전 답과 새 입력을 나눔
2. **정지 신호** — 생성 중 `<사용자>`/`<봇>` 이 나오면 "다음 차례"로 보고 멈춤
3. **문맥이 길어질 때** — `ORDERS` 에 3을 넣어 앞 3토큰을 보면 그때 문맥에 들어옴

### 그래도 둘 다 두는 이유

- 실제 챗 모델도 역할 토큰을 써요 — ChatGPT 계열 `<|im_start|>user`/`assistant`(ChatML), Llama `[INST]`.
- **v0.0.8 의 목적이 이 "대화 형식(chat template)" 을 흉내 내는 것**이라, 표준 방식을 따릅니다.
- 정리: **`<봇>` 은 값을 하고, `<사용자>` 는 구조·표준을 위한 것**이며, `<사용자>` 가 제 값을 하는 건
  문장 전체를 보는 **트랜스포머(v0.3.0~)** 부터예요.

## 사용 방법

```bash
python3 1.train/train.py     # 1) 대화 데이터 학습
python3 2.test/test.py       # 2) 여러 턴 주고받는 대화 데모
```

웹앱(`web_service`)에서 **v0.0.8 을 고르면 진짜 여러 턴 대화**가 이어져요.
(v0.0.7 이하는 기존처럼 한 문장씩 이어 붙입니다.)

## 한계 (→ 다음 예고)

우리 모델은 앞 **1~2 토큰**만 보는 n-gram이라 **먼 과거를 잘 못 봐요.**
예를 들어 질문이 모두 `?` 로 끝나면 문맥 `? <봇>` 이 겹쳐 답이 섞일 수 있어요.
진짜 '긴 대화 기억' 은 나중에 **트랜스포머**(셀프어텐션) 에서 다뤄집니다.

## 버전

- **v0.0.1** 앞 단어 1개 / **v0.0.2** 앞 단어 2개 / **v0.0.3** 문장 끝 `<END>`
- **v0.0.4** 백오프 / **v0.0.5** 확률+온도 / **v0.0.6** 문장부호 토크나이저
- **v0.0.7** top-k / top-p 샘플링
- **v0.0.8** 대화 형식(`<사용자>`/`<봇>`) + 멀티턴 문맥 ← 현재
