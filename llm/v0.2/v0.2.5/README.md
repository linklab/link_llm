# link_llm — 나만의 작은 언어 모델 (v0.2.5)

**3자 비교 (캡스톤).** 모델이 아니라 **'질문'이 새 개념**이에요.
모델·학습은 v0.2.4 그대로이고, 임베딩 시대를 열며 내건 약속을 실제로 재봅니다.

> v0.2.0 의 주장: *"one-hot 은 처음 보는 조합에 무력하지만,
> 임베딩은 비슷한 토큰이 비슷한 벡터라 **처음 보는 조합에도 일반화**한다."*

## 왜 검증 PPL 하나로는 답할 수 없나

v0.1.5 의 대결표는 버전마다 **숫자 하나**를 냈어요. 그런데 그 숫자는 성격이 전혀 다른 자리를
**섞어서 평균**낸 값입니다.

| 칸 | 조건 | 성격 |
|---|---|---|
| ① **본 조합** | 그 문맥에서 그 다음 토큰이 학습에 **있었음** | 외우기만 해도 맞힘 |
| ② **새 조합** | 문맥은 봤지만 그 조합은 **처음** | ★ 일반화가 필요 |
| ③ **새 문맥** | 문맥 자체가 **처음** | ★★ 가장 어려움 |

- 판정은 **모델과 무관**하게 데이터만 보고 해요 — 학습 문장의 `(앞 2토큰, 다음 토큰)` 과 대조할 뿐.
- 앞 2토큰은 세 모델(카운트 2 · one-hot 2 · 임베딩 3)의 **공통 분모**라 어느 쪽도 편들지 않아요.

## 검증 자리는 이렇게 생겼어요

| 칸 | 자리 수 | 비율 | 전체 손실에서의 몫 |
|---|---|---|---|
| ① 본 조합 | 7,717 | **96.3%** | 79~82% |
| ② 새 조합 | 135 | 1.7% | 9~11% |
| ③ 새 문맥 | 164 | 2.0% | 9~11% |
| 합계 | 8,016 | 100% | 100% |

> ⚠️ **검증 자리의 96%가 이미 본 조합이에요.** 그래서 전체 PPL 은 사실상 **암기 점수**에 가깝고,
> "처음 보는 것에 강한가"는 남은 **3.7%** 에서만 드러납니다. 그 3.7% 가 전체 손실의 약 **20%** 를
> 차지하는데도 평균에 묻혀 안 보였던 거예요.

## 3자 대결표 (칸별 검증 PPL, 낮을수록 좋음)

```
버전     표현                    전체   ①본 조합   ②새 조합   ③새 문맥    파라미터
v0.0.9   개수 표 (표현 없음)      2.96      2.43     935.59     274.77       9,512
v0.1.5   one-hot V차원 (독립)     2.90      2.45     619.61     108.54     942,281
v0.2.5   임베딩 E차원 (공유)     3.25      2.73     734.71     132.39     207,305
```

## 읽는 법

**① 전체 PPL 은 거의 '암기 점수'예요**
- 세 모델의 전체 PPL 이 2.90~3.25 로 붙어 있는 건 96% 자리에서 다 비슷하기 때문.
- 카운트가 신경망과 대등해 보이는 이유가 여기 있어요 — **이 데이터가 외우기 쉬워서**입니다.

**② 처음 보는 자리에서는 PPL 이 100~900 으로 폭발해요**
- ① 2.4 → ② 620~936 · ③ 109~275. **같은 모델, 같은 지표인데 자리가 다를 뿐**이에요.
- '언어 모델이 잘한다'는 말이 얼마나 자리에 따라 달라지는지 보여주는 숫자입니다.

**③ 신경망은 카운트를 크게 이기지만, 그건 one-hot 때부터 이미 그랬어요**
- ② 새 조합: 카운트 **935.59** vs 신경망 **619.61 / 734.71** — 신경망 압승.
- ③ 새 문맥: 카운트 **274.77** vs 신경망 **108.54 / 132.39** — 역시 신경망 압승.
- 카운트는 표에 없으면 백오프로 1토큰까지 줄이고, 그래도 없으면 `FLOOR` 예요. **일반화 수단이 없어요.**

**④ 그런데 임베딩이 one-hot 보다 더 잘 일반화하지는 않았어요 — 주장 미확인**
- ②③ 어느 칸에서도 임베딩이 one-hot 을 넘지 못합니다.
- 용량을 맞춘 대조로 다시 봐도 같아요 (둘 다 **앞 2토큰**):

  | | 표현 | 파라미터 | ② 새 조합 | ③ 새 문맥 |
  |---|---|---|---|---|
  | v0.1.5 | one-hot | 942,281 | **619.61** | **108.54** |
  | v0.2.0 | 임베딩 | 759,753 | 894.49 | 116.49 |

- ⇒ **이 데이터에서는 v0.2.0 의 주장이 확인되지 않아요.** 어휘 1,300 · 학습 3,600문장은
  one-hot 이 아직 충분히 감당하는 규모라, 압축·공유의 이득이 나타날 자리가 없습니다.
- v0.2.2 의 교훈과 같은 결론이에요 — **모든 기법이 모든 규모에서 통하진 않아요.**

**⑤ 임베딩이 실제로 준 것: 효율**
- 임베딩 **207,305** 파라미터로 one-hot **942,281** 의 4.5분의 1. (weight tying 이 V×H 를 돌려줌)
- 같은 임베딩끼리 비교하면 문맥·튜닝의 이득은 분명해요 —
  v0.2.0(2토큰, 760K)의 ② 894.49 → v0.2.5(3토큰, 207K)의 ② 734.71.
  **파라미터를 3.7배 줄이고도 처음 보는 조합에서 더 낫습니다.**

**⑥ '정답이 후보에 있기라도 한가'**
- 카운트 **97.1%** vs 신경망 **98.9%**. 카운트는 표에 없는 조합을 **아예 만들지 못해요.**
- PPL 에서는 `FLOOR` 가 메워 가려지는 한계입니다.

## 공정한 비교인 이유

- 세 모델 모두 같은 토크나이저(punct) · 같은 `prepare`(`<END>`) · 같은 `FLOOR` · **같은 채점 자리**.
- 칸 이름표는 **데이터만** 보고 붙여요 (모델을 안 봄).
- 자리별 `log p` 를 평균 내면 그 버전 `perplexity()` 와 **소수점 15자리까지 일치** — 표는 기존 숫자의
  **분해**이지 새로 만든 지표가 아니에요.
- 자리 수가 어긋나는 버전(토크나이저가 다른 v0.0.1~0.0.7)은 자동으로 **건너뜁니다**.

## 실행

> ⚠️ **PyTorch** 필요: `pip install torch`
> 비교할 버전들은 각자 `1.train/train.py` 로 **먼저 학습**해 두세요.

```bash
python3 1.train/train.py     # v0.2.5 자신을 학습
python3 2.test/test.py       # 3자 대결표 출력
```

- `2.test/test.py` 의 `VERSIONS` 를 바꾸면 다른 버전도 표에 넣을 수 있어요.
- `0.model/lm.py` 의 `NOVELTY_CONTEXT` 를 3 으로 바꾸면 판정 문맥이 길어져 ②③ 자리가 늘어요.

## 학습 설정 — `EPOCHS` 만 v0.2.4 와 달라요

모델·손실·저장은 v0.2.4 그대로인데 `EPOCHS` 만 **1,500 → 300** 으로 낮췄어요.
v0.2.3 이 진단한 결함을 **실제로 고치는 자리**입니다.

| `EPOCHS` | 같은 설정 3회 | 편차 | 멈춘 epoch |
|---|---|---|---|
| 1,500 | 3.26 · 3.59 · 3.66 | **0.40** | 72 / 173 / 1198 |
| **300** | 3.2535 · 3.2511 · 3.2544 | **0.003** | 239 / 268 / 271 |

- `EPOCHS` 는 cosine 의 `T_max` 를 겸해서 **상한**이자 **감쇠 기간**이에요.
  1,500 이면 학습률이 한참 안 줄어 **조기 종료가 언제 걸리느냐가 우연**이 됩니다.
- 300 이면 감쇠가 예산 안에서 끝나 매번 깊이까지 학습하고 멈춰요 → **편차가 130배 줄어요.**
- 캡스톤은 세 시대의 대표를 공정하게 비교하는 자리라, **임베딩 대표가 운에 좌우되면 안 돼요.**

> 💡 **교훈: 재현되지 않는 숫자로는 비교할 수 없어요.** 하이퍼파라미터 하나가 두 역할을 겸하면
> 이런 일이 생깁니다 — 제대로 된 해법은 `T_max` 를 `EPOCHS` 에서 **분리**하는 것이고,
> 여기서는 코드를 안 바꾸는 선에서 **감쇠가 끝나는 값을 고르는 것**으로 대신했어요.

## 남은 한계 → v0.3 (트랜스포머)

세 모델의 공통점은 **문맥이 짧고 고정**이라는 거예요.

- 카운트는 표가 폭발해서, MLP 는 입력층 크기가 `N·E` 로 고정이라 문맥을 길게 못 늘려요.
- 게다가 MLP 는 앞 N토큰을 **한 줄로 이어붙일** 뿐, 토큰끼리 무엇이 중요한지 **서로 보지 못해요.**
- ③ 새 문맥의 PPL 이 100 을 넘는 건 이 한계와 맞닿아 있습니다.

> 다음 **v0.3.x** 는 셀프어텐션으로 이걸 풉니다 — 문맥 안의 토큰들이 **서로를 보고**
> 중요한 것에 가중치를 주는 구조. 진짜 GPT 의 시작이에요.

## 임베딩 시대 완성 (v0.2.x, 6단계)

| 버전 | 한 걸음 | 검증 PPL |
|---|---|---|
| v0.2.0 | 임베딩 도입 | 2.94 |
| v0.2.1 | 문맥 확장 (`block_size`) | 3.37 |
| v0.2.2 | 초기화·정규화 | 3.46 |
| v0.2.3 | 일반화·튜닝 | 3.59 |
| v0.2.4 | 임베딩 시각화 (PCA · t-SNE · 최근접이웃) | 3.26 |
| **v0.2.5 ← 현재** | 3자 비교 (캡스톤) · **임베딩 시대 완성** | 3.25 |

> 다음은 **v0.3.x** — 셀프어텐션으로 문맥 안 토큰들이 서로를 보게 만듭니다 (미니 GPT).
