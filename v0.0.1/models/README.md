# models 폴더

이 폴더에는 학습이 끝난 **모델 파일(`model.json`)** 이 저장됩니다.

- `model.json` 은 `train/train.py` 를 실행하면 자동으로 만들어져요.
- 내용은 "어떤 단어 뒤에 어떤 단어가 몇 번 나왔는지" 를 적어둔 표(JSON)입니다.

예시:

```json
{
  "나는": { "밥을": 1, "학교에": 1, "코딩을": 1 },
  "오늘": { "날씨가": 1, "기분이": 1 }
}
```
