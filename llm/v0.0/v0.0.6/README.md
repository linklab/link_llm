# link_llm — 나만의 작은 언어 모델 (v0.0.6)

신경망 없이 **단어를 세어** 문장을 만드는 언어 모델이에요. (Python 3 만 있으면 실행)
어려운 신경망(딥러닝) 없이, **"토큰을 세는 것"** 으로 문장을 만들어 봅니다.

## v0.0.5 → v0.0.6, 무엇이 바뀌었나요?

> **토크나이저(문장을 토막 내는 규칙)를 똑똑하게 바꿨어요.**
> (백오프 + 확률/온도(v0.0.5)는 그대로 유지해요.)

- v0.0.5 까지는 **띄어쓰기**로만 잘랐어요. 그래서 `"좋아!"` 가 통째로 한 덩어리라,
  `"좋아"` 와 `"좋아!"` 를 **서로 다른 단어**로 여기는 손해가 있었죠.
- v0.0.6 은 **문장부호(. , ! ?) 를 따로 떼어내서** 각각 하나의 토큰으로 만들어요.

```
"좋아!"        ->  ["좋아", "!"]
"먹고, 잤어."  ->  ["먹고", ",", "잤어", "."]
```

이렇게 하면 `"좋아"` 는 항상 같은 토큰이 되고, **문장부호가 언제 나오는지도** 배울 수 있어요.
문장을 만들 때는 문장부호를 앞 단어에 딱 붙여서 보여줘요 → `오늘 날씨가 좋습니다!`

## 지금까지 쌓인 기능 (누적)

| 기능 | 도입 버전 | 유지? |
|------|----------|-------|
| 앞 2단어 문맥 | v0.0.2 | ✅ |
| 문장 끝(`<END>`) | v0.0.3 | ✅ |
| 백오프(2→1토큰) | v0.0.4 | ✅ |
| 확률 + 온도 | v0.0.5 | ✅ |
| 문장부호 토크나이저 | **v0.0.6** | ✅ (현재) |

## 저장되는 모델 형식 (v0.0.6)

`tokenizer` 와 백오프용 `tables` 를 함께 저장해요.

```json
{
  "max_order": 2,
  "tokenizer": "punct",
  "tables": {
    "2": { "오늘 날씨가": { "정말": 1, "조금": 1 }, "좋습니다 !": { "<END>": 1 } },
    "1": { "오늘": { "날씨가": 3, "기분이": 3 }, "!": { "<END>": 8 } }
  }
}
```

## 사용 방법

```bash
python3 1.train/train.py     # 1) 학습 (문장부호 분리 + 백오프용 표1, 표2)
python3 2.test/test.py       # 2) 문장을 만들어 보기 (문장부호가 자연스럽게 나옴)
```

웹앱(`web_service`)은 각 버전의 `tokenizer` 정보를 읽어 자동으로 맞는 방식으로 처리해요.

## 개수 세기 시대 진행 (v0.0.x, 9단계)

| 버전 | 한 걸음 |
|---|---|
| v0.0.1 | 앞 단어 1개로 예측 |
| v0.0.2 | 앞 단어 2개로 예측 |
| v0.0.3 | 문장 끝 `<END>` 학습 |
| v0.0.4 | 백오프 (2→1단어 재시도) |
| v0.0.5 | 개수 → 확률 + 온도 |
| **v0.0.6 ← 현재** | 문장부호 토크나이저 |
| v0.0.7 | top-k / top-p 샘플링 |
| v0.0.8 | 역할 토큰 · 멀티턴 문맥 기계 |
| v0.0.9 | 퍼플렉서티 평가 (시대 완성) |
