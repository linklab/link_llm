# link_lmm

언어 모델(LMM)을 From Scratch 로, 버전별로 관리하고 웹앱에서 테스트하는 프로젝트입니다.

## 폴더 구조

버전들은 **마이너별 그룹 폴더**(`v0.0`, `v0.1` …) 아래에, 그 안에 각 패치 버전이 들어 있어요.

```
link_lmm/
├── lmm/                    # 모델 버전들을 모아두는 곳
│   ├── v0.0/               # [개수 세기 시대] 마이너 그룹
│   │   ├── v0.0.1/         #   - 앞 단어 1개로 예측 (가장 기본)
│   │   │   ├── 1.data/       #      data.txt (학습용 문장)
│   │   │   ├── 2.models/     #      lm.py (NGramLM + 설정) + model.json (학습 결과)
│   │   │   ├── 3.train/      #      얇은 train.py
│   │   │   └── 4.test/       #      얇은 test.py
│   │   ├── v0.0.2/         #   - 앞 단어 2개 (lm.py 가 v0.0.1 을 상속, 새 코드 없음)
│   │   ├── v0.0.3/         #   - 문장 끝(<END>) 학습    (lm.py 가 <END> 추가)
│   │   ├── v0.0.4/         #   - 백오프(2→1단어 재시도)  (lm.py 가 백오프 추가)
│   │   ├── v0.0.5/         #   - 개수→확률 + 온도        (lm.py 가 온도 추가)
│   │   ├── v0.0.6/         #   - 문장부호 토크나이저      (lm.py 가 토크나이저 교체)
│   │   ├── v0.0.7/         #   - top-k / top-p 샘플링     (lm.py 가 choose 자르기 추가)
│   │   ├── v0.0.8/         #   - 대화 형식 + 멀티턴        (lm.py 가 chat() 추가)
│   │   └── v0.0.9/         #   - 퍼플렉서티 평가 + 데이터 정비 (개수 세기 시대 완성)
│   ├── v0.1/               # [신경망 시대] 2층 MLP, one-hot 입력 (마이너 그룹)
│   │   ├── v0.1.0/         #   - 신경망 + PyTorch autograd (2층 MLP, model.pt/vocab.json 저장)
│   │   ├── v0.1.1/         #   - Dataset·DataLoader 미니배치 학습
│   │   ├── v0.1.2/         #   - torch.optim 옵티마이저 (수동 갱신 → optimizer.step)
│   │   ├── v0.1.3/         #   - 정규화·초기화 (weight decay, init, label smoothing)
│   │   ├── v0.1.4/         #   - 2토큰 문맥 신경망 (카운트와 같은 문맥으로 공정한 대결)
│   │   └── v0.1.5/         #   - 기준선 대결(캡스톤): 카운트 vs 신경망 PPL 비교
│   └── v0.2/               # [임베딩 시대] one-hot → 임베딩 (마이너 그룹)
│       └── v0.2.0/         #   - 임베딩 도입 (nn.Embedding, Bengio 입력층)
├── web_service/            # ChatGPT 스타일 웹앱 (선택한 버전의 lm.py 를 불러와 재사용)
│   ├── server.py
│   └── index.html
└── README.md
```

> 상속·로딩은 그룹을 자동으로 찾아가요: 각 `lm.py` 의 `_load_prev` 는 이전 버전 이름
> (예: `v0.0.9`)에서 그룹(`v0.0`)을 계산해 `lmm/v0.0/v0.0.9/…` 경로로 불러오고,
> `web_service` 도 `lmm/<그룹>/<버전>/2.models/model.json` 을 훑어 버전을 찾습니다.

## 코드 구조 (버전별 2.models/lm.py + 상속 사슬)

버전마다 코드가 거의 똑같이 반복되던 것을, **상속 사슬**로 정리했어요.
각 버전의 `2.models/lm.py` 하나에 **NGramLM(기능) + Model(설정)** 이 함께 들어 있고,
그 버전이 **처음 추가한 함수만** 담아 나머지는 이전 버전에서 물려받아요.

```
v0.0.1/2.models/lm.py  NGramLM (기반: 읽기·학습·저장·생성 전부) + Model
      ▲ 상속
v0.0.2/2.models/lm.py  (새 코드 없음 — ORDERS=[2] 설정만으로 앞 2단어)
      ▲
v0.0.3/2.models/lm.py  + prepare()/is_end()      ← 문장 끝 <END>
      ▲
v0.0.4/2.models/lm.py  + next_token()/can_continue()  ← 백오프
      ▲
v0.0.5/2.models/lm.py  + choose()                ← 확률 + 온도
      ▲
v0.0.6/2.models/lm.py  + tokenize()/detokenize() ← 문장부호 분리
      ▲
v0.0.7/2.models/lm.py  + choose()/generate()     ← top-k / top-p 샘플링
      ▲
v0.0.8/2.models/lm.py  + chat()/build_chat_context()  ← 대화 형식 + 멀티턴
      ▲
v0.0.9/2.models/lm.py  + token_prob()/perplexity()    ← 퍼플렉서티 평가
```

- **각 버전 `2.models/lm.py`** — `NGramLM`(그 버전이 새로 추가/변경한 함수) + `Model`(설정 `ORDERS`).
  → "이 버전이 뭘 더했나"가 한눈에.
- **`3.train/train.py`·`4.test/test.py`** — `2.models/lm.py` 를 불러와 실행하는 **얇은 껍데기**.
- **`web_service/server.py`** — 선택된 버전의 `2.models/lm.py` 를 불러와 `generate` 만 호출.

> 폴더 이름에 `.` 이 있어 일반 `import` 가 안 되므로, 이전 버전 `lm.py` 는 파일 경로로
> 불러와요(각 `lm.py` 상단의 `_load_prev`). "이 버전이 뭘 바꿨나"는 그 `lm.py` 하나만,
> "전체가 어떻게 도나"는 `v0.0.1/2.models/lm.py` 부터 사슬을 따라 읽으면 됩니다.
> 모델 저장 형식은 개수 세기(v0.0.x) 버전은 `{ "max_order", "tokenizer", "tables" }` 를 담은
> `model.json` 하나예요. 신경망(v0.1.x~) 버전은 **PyTorch 표준**으로 나눠 저장해요 —
> 가중치는 `model.pt`(`torch.save(net.state_dict())`), 어휘는 `vocab.json`(`{tokenizer, vocab}`).
> 불러올 때 저장된 가중치 모양에서 은닉 크기를 복원해 신경망을 다시 만들고 `load_state_dict` 로 채웁니다.

## 클래스 이름은 왜 `NGramLM` 인가요?

`NGramLM` 은 **N-gram + LM** 을 합친, NLP(자연어처리)의 **표준 용어**예요.

- **N-gram** — "연속된 n개의 토큰(단어) 묶음". 우리 모델이 하는 일이 바로
  *"앞의 몇 개 토큰을 보고 다음 토큰을 맞히기"* 라, 토큰 묶음의 등장 횟수를 세는
  **n-gram 언어모델** 이에요.
- **LM** — **Language Model(언어 모델)**. 다음 토큰의 확률을 매기는 모델의 표준 약자.

즉 `NGramLM` = **"N-그램 언어 모델"** (교과서에 그대로 나오는 이름).

**왜 이 이름을 골랐나**

1. **하는 일을 정확히 표현** — 신경망이 아니라 "n-gram을 세는" 방식임을 이름만으로 알 수 있어요.
2. **모든 버전을 아우름** — `ORDERS` 설정만 바꾸면 앞 1단어·2단어·백오프를 다 처리하므로,
   특정 버전에 묶인 `BigramModel` 같은 이름보다 일반적인 `NGram` 이 맞아요.
   (참고: `ORDERS` 는 '문맥 길이'. `order=1` 은 사실 2-gram, `order=2` 는 3-gram — n = 문맥 길이 + 1)
3. **표준 용어라 '진짜'와 연결** — 나중에 논문·교재를 볼 때 바로 이어져, 교육용으로 좋아요.
4. **신경망 시대와 대비** — v0.1.x 에서 신경망을 도입하면 `NeuralLM`·`MiniGPT` 같은 다른 이름을 쓸
   거예요. `NGramLM` 이라는 이름이 **"개수 세기 시대의 모델"** 임을 분명히 표시해 줍니다.

> 참고: README 본문은 언어 모델을 `LMM` 으로 줄여 쓰지만, 코드 클래스명은 관례적인 표준 약자
> `LM`(Language Model)을 따릅니다.

## 버전 목록

- [**v0.0.1**](lmm/v0.0/v0.0.1/) — "단어 세기"만으로 문장을 만드는 아주 쉬운 언어 모델
  (앞 단어 1개로 예측). 자세한 설명은 [lmm/v0.0/v0.0.1/README.md](lmm/v0.0/v0.0.1/README.md) 참고.
- [**v0.0.2**](lmm/v0.0/v0.0.2/) — 앞 단어 **2개**로 예측해 문장이 더 자연스러워졌고,
  학습 데이터를 더 다양하고 많게 늘렸어요. 자세한 설명은 [lmm/v0.0/v0.0.2/README.md](lmm/v0.0/v0.0.2/README.md) 참고.
- [**v0.0.3**](lmm/v0.0/v0.0.3/) — 문장 끝(`<END>`)을 학습해, 정해진 길이에서 툭 끊기지 않고
  **자연스럽게 마무리**돼요. 자세한 설명은 [lmm/v0.0/v0.0.3/README.md](lmm/v0.0/v0.0.3/README.md) 참고.
- [**v0.0.4**](lmm/v0.0/v0.0.4/) — **백오프**: 앞 2단어 문맥이 없으면 1단어로 줄여 다시 찾아요.
  덕분에 **1단어만 입력해도** 그 단어로 시작하는 문장을 만들 수 있어요.
  자세한 설명은 [lmm/v0.0/v0.0.4/README.md](lmm/v0.0/v0.0.4/README.md) 참고.
- [**v0.0.5**](lmm/v0.0/v0.0.5/) — 개수를 **확률**로 바꾸고 **온도(temperature)** 로 다양성을 조절해요
  (실제 GPT의 문장 생성 방식). 웹앱에 온도 슬라이더가 있어요.
  자세한 설명은 [lmm/v0.0/v0.0.5/README.md](lmm/v0.0/v0.0.5/README.md) 참고.
- [**v0.0.6**](lmm/v0.0/v0.0.6/) — **토크나이저 개선**: 문장부호(. , ! ?)를 따로 떼어 학습해
  `"좋아"` 와 `"좋아!"` 를 같은 단어로 다루고, 문장부호가 언제 나오는지도 배워요.
  자세한 설명은 [lmm/v0.0/v0.0.6/README.md](lmm/v0.0/v0.0.6/README.md) 참고.
- [**v0.0.7**](lmm/v0.0/v0.0.7/) — **top-k / top-p 샘플링**: 뽑기 전에 확률 낮은 '꼬리 후보'를
  잘라내 안정성을 높여요. 확률 분포에만 작용해 나중에 신경망에서도 그대로 재사용됩니다.
  자세한 설명은 [lmm/v0.0/v0.0.7/README.md](lmm/v0.0/v0.0.7/README.md) 참고.
- [**v0.0.8**](lmm/v0.0/v0.0.8/) — **대화 형식 + 멀티턴**: 역할 토큰(`<사용자>`/`<봇>`)으로 대화를
  학습하고, 이전 대화를 문맥으로 이어받아 답해요 (ChatGPT 골격). 웹앱이 여러 턴 대화가 됩니다.
  자세한 설명은 [lmm/v0.0/v0.0.8/README.md](lmm/v0.0/v0.0.8/README.md) 참고.
- [**v0.0.9**](lmm/v0.0/v0.0.9/) — **퍼플렉서티 평가 + 데이터 정비**: 학습/검증 데이터를 나눠
  "얼마나 잘 맞히나"를 하나의 숫자(PPL)로 재요. **개수 세기 시대(v0.0.x)의 완성**이자,
  신경망과 비교할 기준선. 자세한 설명은 [lmm/v0.0/v0.0.9/README.md](lmm/v0.0/v0.0.9/README.md) 참고.
- [**v0.1.0**](lmm/v0.1/v0.1.0/) — **신경망 bigram + PyTorch autograd**: 개수 세기 대신 가중치 `W` 를
  경사하강으로 **학습**해요(미분은 autograd 가 대신). 확률 엔진만 신경망으로 바꾸고 나머지(대화·샘플링·PPL)는
  그대로라 웹앱이 그대로 로드해요. **신경망 시대(v0.1.x)의 시작.** (실행에 `pip install torch` 필요)
  자세한 설명은 [lmm/v0.1/v0.1.0/README.md](lmm/v0.1/v0.1.0/README.md) 참고.
- [**v0.1.1**](lmm/v0.1/v0.1.1/) — **미니배치·벡터화 학습**: 모델(신경망 bigram)은 v0.1.0 그대로이고,
  학습만 **full-batch → 미니배치 SGD**(매 에폭 셔플 + 배치 단위 갱신)로 바꿨어요. 저장 형식이 같아
  웹앱이 그대로 로드하고, PPL 이 v0.1.0 과 비슷하면 "방법만 바꿔도 결과는 같다"가 확인돼요.
  자세한 설명은 [lmm/v0.1/v0.1.1/README.md](lmm/v0.1/v0.1.1/README.md) 참고.
- [**v0.1.2**](lmm/v0.1/v0.1.2/) — **옵티마이저(`torch.optim`)**: 손으로 하던 파라미터 갱신을
  `optimizer.zero_grad() → backward() → optimizer.step()` 표준 3줄로. `OPTIMIZER` 로 **SGD / momentum / Adam**
  을 골라 수렴을 비교해요. 모델·데이터·저장 형식은 그대로. 자세한 설명은 [lmm/v0.1/v0.1.2/README.md](lmm/v0.1/v0.1.2/README.md) 참고.
- [**v0.1.3**](lmm/v0.1/v0.1.3/) — **정규화·초기화**: `weight_decay`(L2)·가중치 초기화(·label smoothing)로
  **과적합을 눌러 일반화**를 개선해요. 목표가 손실↓ 이 아니라 **학습 vs 검증 PPL 격차↓** (v0.0.10 스무딩과 같은 정신).
  모델·저장 형식은 그대로. 자세한 설명은 [lmm/v0.1/v0.1.3/README.md](lmm/v0.1/v0.1.3/README.md) 참고.
- [**v0.1.4**](lmm/v0.1/v0.1.4/) — **2토큰 문맥 신경망**: 지금까지 앞 1토큰만 보던 신경망을 **앞 2토큰**으로 넓혀
  (one-hot 2개 concat → `nn.Linear(2V, V)`), 카운트(v0.0.9, 앞 2토큰)와 **같은 문맥의 공정한 대결**로 만들어요.
  softmax+label smoothing 의 부드러움으로 카운트의 FLOOR 절벽을 피해 검증 PPL 을 낮춥니다.
  자세한 설명은 [lmm/v0.1/v0.1.4/README.md](lmm/v0.1/v0.1.4/README.md) 참고.
- [**v0.1.5**](lmm/v0.1/v0.1.5/) — **기준선 대결(캡스톤)**: 같은 학습/검증 데이터로 **v0.0.9(카운트) vs 신경망(1토큰·2토큰)**
  의 PPL 을 나란히 재서 "신경망이 정말 이겼나?"를 확인해요. `4.test/test.py` 가 대결표를 출력합니다.
  **신경망 시대(v0.1.x) 완성.** 자세한 설명은 [lmm/v0.1/v0.1.5/README.md](lmm/v0.1/v0.1.5/README.md) 참고.
- [**v0.2.0**](lmm/v0.2/v0.2.0/) — **임베딩 도입**: 입력을 one-hot → **`nn.Embedding`** 으로 바꿔요.
  `nn.Embedding(V,E)(idx) == one_hot(idx) @ C` 동치이지만 **E ≪ V 로 압축·공유**돼, 비슷한 토큰이
  비슷한 벡터를 갖게 학습 → 문맥끼리 강점 공유. 2토큰 문맥·2층 MLP·저장방식은 v0.1.5 그대로.
  **임베딩 시대(v0.2.x)의 시작.** 자세한 설명은 [lmm/v0.2/v0.2.0/README.md](lmm/v0.2/v0.2.0/README.md) 참고.

## 🗺️ 앞으로의 계획 (로드맵)

여기까지가 **개수 세기 시대(v0.0.x)** 예요. 다음은 신경망으로 넘어갑니다.

> **핵심 원칙 — 인터페이스는 고정, '확률 엔진'만 교체.** v0.0.x에서 만든 토크나이저·대화 템플릿(`chat`)·
> 샘플링(온도/top-k·top-p)·생성(`generate`)·퍼플렉서티(PPL)는 그대로 두고, 각 신경망 버전은
> **"다음 토큰 확률을 어디서 얻느냐"(개수 표 → 신경망)만** 바꿔요. **v0.1.0부터 PyTorch autograd 사용.**
>
> 덕분에 **모든 버전이** `1.data / 2.models / 3.train / 4.test` 구조 그대로라 `web_service` 가 수정 없이 로드하고,
> **학습 → 생성/대화 → PPL 측정**까지 완결되어 **웹앱에서 바로 평가**할 수 있어요.
> (각 마이너의 마지막은 여러 모델을 웹앱에서 나란히 비교하는 **캡스톤** 버전이에요.)

### v0.1.x — 신경망 언어모델 (임베딩 없음, 6단계 · 전부 웹앱 완결)

개수 세기 → **PyTorch autograd 학습**으로 원리가 바뀌는 구간이에요.

| 버전 | 한 걸음 | 완결 모델 (웹앱에서) |
|------|--------|---------------------|
| **v0.1.0** | 신경망 bigram + autograd | 데이터 준비 → forward → `F.cross_entropy` → `backward()` → 학습 루프 → 생성/대화까지 **한 버전에 완결**. 첫 신경망 모델 |
| **v0.1.1** | 미니배치·벡터화 학습 | 같은 bigram을 배치·벡터화로 학습. 동작 동일, PPL로 동등 성능 확인 |
| **v0.1.2** | 옵티마이저(`torch.optim`) | SGD→momentum→Adam. 수렴 개선 → 검증 PPL 확인 |
| **v0.1.3** | 정규화·초기화 | weight decay·초기화·label smoothing → 검증 PPL 개선 |
| **v0.1.4** | **2토큰 문맥 신경망** | 문맥 1→2토큰(`nn.Linear(2V,V)`). 카운트와 같은 문맥으로 공정하게 |
| **v0.1.5** | **기준선 대결 (캡스톤)** | 웹앱에서 v0.0.9(카운트) vs 신경망(1토큰·2토큰)을 PPL로 나란히 — "신경망이 정말 이겼나" |

### v0.2.x — 임베딩 + 문맥 확장 (Bengio, 6단계 · 전부 웹앱 완결)

one-hot → **임베딩 벡터**로. (은닉층 2층 MLP는 v0.1.x에서 이미 도입 → v0.2.x는 **표현·문맥**에 집중.)
역시 모든 버전이 웹앱에서 대화·평가 가능해요.

| 버전 | 한 걸음 | 완결 모델 (웹앱에서) |
|------|--------|---------------------|
| **v0.2.0** | 임베딩 도입 ✅ | one-hot → `nn.Embedding`. `Embedding(V,E)(i)==one_hot(i)@C` (E≪V 압축·공유) |
| **v0.2.1** | 문맥 확장(`block_size`) | 앞 N토큰(2→3→…) 임베딩 concat = 진짜 Bengio 입력층 |
| **v0.2.2** | 초기화·정규화 | Kaiming/BatchNorm/weight decay → 깊은 MLP 안정·개선 |
| **v0.2.3** | 일반화·튜닝 | dropout·weight tying·early stopping, LR 스케줄·sweep |
| **v0.2.4** | 임베딩 시각화 | 모델은 그대로 + PCA/t-SNE·nearest-neighbor 분석 |
| **v0.2.5** | **3자 비교 (캡스톤)** | 웹앱에서 카운트 vs one-hot 신경망 vs 임베딩 MLP 3자 PPL → v0.3 예고 |

> 💡 이제 **모든 버전이 자체 PPL로 평가 가능**하므로, 독립 평가 버전은 각 마이너의 마지막
> **비교 캡스톤**(v0.1.5·v0.2.5)으로 남겼어요 — 여러 모델을 웹앱에서 나란히 벤치마크하는 버전이에요.

### 그 이후

- **v0.3.x** — 셀프어텐션 → **미니 트랜스포머** (진짜 GPT 구조; 위치 임베딩 추가)
- **v1.0.0** — 트랜스포머 + 대화 = **"ChatGPT처럼"**

## 웹앱 실행

```bash
python3 web_service/server.py
```

브라우저에서 접속:

```
http://127.0.0.1:8000
```

우측 상단에서 **모델 버전**을 고르고 메시지를 보내면, 그 버전이 학습한 내용을
바탕으로 대답을 만들어 줘요. `lmm/` 안에 새 버전 폴더를 만들면 자동으로 목록에 추가됩니다.

## 모르는 단어가 입력되면?

이 모델은 입력 문장의 **마지막 단어**를 시작점으로 문장을 만들어요.

- 마지막 단어가 모델에 **있으면** → 그 단어부터 시작
- 마지막 단어가 모델에 **없으면**(모르는 단어) → 모델이 아는 단어 중 **무작위로 하나**를 골라 시작

즉, 모르는 단어가 들어와도 오류 없이 항상 문장을 만들어 냅니다(폴백 방식).
대신 입력과 상관없는 엉뚱한 답이 나올 수 있는데, 이는 단어의 *의미*를 이해하는 게 아니라
"어떤 단어 뒤에 무슨 단어가 나왔는지"만 세어둔 아주 단순한 모델이기 때문이에요.

## v0.0.2·v0.0.3 에서 단어를 1개만 입력하면?

v0.0.2·v0.0.3 은 **앞 두 단어**로 다음 단어를 찾는 모델이에요. 그래서 입력이 1개뿐이면
두 단어짜리 문맥을 만들 수 없어, 다음 순서로 처리됩니다.

1. 마지막 두 단어가 필요한데 1개뿐 → 문맥을 못 만듦(빈 문맥)
2. 빈 문맥은 표에 없으므로 → **폴백**: 모델이 아는 두 단어 문맥 중 **무작위로 하나**를 골라 시작
3. 그 문맥부터 단어를 이어 붙여 문장을 완성 (v0.0.3 은 `<END>` 에서 자연스럽게 멈춤)

즉, **입력한 한 단어는 결과에 나오지 않고 무시**되며(무작위 시작), 시작 방식은 두 버전이 같고
**끝맺음만** 다릅니다(v0.0.2 는 툭 끊길 수 있고, v0.0.3 은 자연스럽게 마무리).

> ✅ **v0.0.4 부터 해결됨**: v0.0.4 의 **백오프**(앞 2단어가 없으면 1단어로 줄여 재시도) 덕분에,
> `"나는"` 한 단어만 입력해도 `"나는"` 으로 시작하는 문장을 만들 수 있어요. 더 이상 입력이 무시되지 않아요.

### 백오프 전후 비교 (1단어 `"나는"` 입력)

| 버전 | 1단어 "나는" 입력 결과 | 해석 |
|------|----------------------|------|
| v0.0.2 / v0.0.3 (백오프 X) | "단풍이 곱게 물듭니다" / "기분이 조금 나쁩니다" | 2단어 문맥 못 만들어 **입력 무시, 무작위 시작** |
| v0.0.4 / v0.0.5 / v0.0.6 (백오프 O) | "나는 아침에 밥을 먹습니다" 등 | 1단어로 줄여 찾아 **입력을 반영** ✅ |

## 새 버전을 만들려면?

`lmm/v0.0.2` 처럼 `lmm/` 아래에 새 폴더를 만들어 관리하면 이전 버전을 그대로 보존할 수 있어요.
(새 버전은 `3.train/train.py`를 한 번 실행해 `2.models/model.json`을 만들어야 웹앱 목록에 나타납니다.)
