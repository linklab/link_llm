# link_llm — 나만의 작은 언어 모델 (v0.2.3)

**일반화 · 튜닝.** 모델 구조·문맥은 v0.2.2 그대로이고, **학습을 다듬는 도구 3개**를 더합니다.

## 한 걸음 — 도구 3개

| # | 도구 | 코드 | 무엇을 하나 |
|---|---|---|---|
| ① | **dropout** | `nn.Dropout(p)` (tanh 뒤) | 학습 중 은닉 뉴런 일부를 꺼 특정 경로 의존을 막음 |
| ② | **weight tying** | `fc2.weight = emb.weight` | 출력층과 임베딩이 **한 텐서를 공유** → 파라미터 V×H 절약 |
| ③ | **LR 스케줄** | `torch.optim.lr_scheduler` | 학습이 진행될수록 학습률을 줄여 곱게 수렴 |

## 구조

```
(앞 N토큰) → 임베딩 concat(N·E) → fc1 → [BatchNorm(선택)] → tanh → [Dropout] → fc2 → logits(V)
                                                                                ↑ (선택) emb.weight 공유
```

- `DROPOUT=0.0` 이면 Dropout 이 항등이라 **v0.2.2 와 완전히 같아져요**.
- **weight tying 은 `HIDDEN == EMBED` 가 필수**입니다 (`fc2.weight`(V,H) 와 `emb.weight`(V,E) 모양이 같아야 함).
  어긋나면 `lm.py` 가 이유를 설명하며 멈춰요.

| `LR_SCHEDULE` | 동작 |
|---|---|
| `"none"` | 고정 |
| `"cosine"` | 코사인 곡선으로 `LR → LR_MIN` **(기본값)** |
| `"step"` | `LR_STEP` 에폭마다 `LR_GAMMA` 배 |
| `"plateau"` | **검증 PPL 이 정체할 때만** 감소 (조기 종료와 같은 신호) |

## 성적 (산문 사전학습)

| | 학습 PPL | 검증 PPL | 격차 | 파라미터 |
|---|---|---|---|---|
| v0.2.1 (N=3) | 2.50 | 2.74 | 1.10배 | 158,121 |
| v0.2.2 (+초기화·정규화) | 2.68 | 2.84 | 1.06배 | 158,121 |
| **v0.2.3 (+튜닝 3종)** | 2.80 | **2.96** | 1.06배 | **166,025** |

- 임베딩을 32 → **128** 로 4배 키웠는데 파라미터는 v0.2.2 와 거의 같아요 —
  weight tying 이 `V×H`(905×128 ≈ **11.6만 개**)를 돌려줬거든요. **같은 예산으로 4배 넓은 표현.**

> ⚠️ **이 숫자는 실행마다 흔들려요.** 위 2.96 은 **현재 저장된 모델**의 값이에요. 아래에서 원인을 봅니다.

## 왜 흔들리나 — `EPOCHS` 가 두 가지 일을 겸해서

`make_scheduler()` 가 cosine 의 감쇠 기간을 `T_max=self.EPOCHS` 로 잡아요. 그래서 `EPOCHS` 는

- **상한** — 최대 몇 에폭까지 돌 수 있나
- **감쇠 기간** — 학습률이 `LR → LR_MIN` 까지 내려가는 데 걸리는 에폭

**두 가지를 동시에** 뜻합니다. `EPOCHS=1500` 이면 학습률이 아주 천천히 줄어서, 한참 동안 높은 채로 머물러요.
그 구간에서는 손실이 출렁이므로 **조기 종료(patience 20)가 언제 걸리느냐가 사실상 우연**이 됩니다.

실측 — 설정이 **똑같은** 두 버전이 이렇게 다른 지점에서 멈춰요:

| 버전 | 멈춘 에폭 | 그때 학습률 | 검증 PPL |
|---|---|---|---|
| v0.2.3 | 24 | 거의 안 줄어듦 | 2.96 |
| v0.2.4 | 42 | 거의 안 줄어듦 | 2.97 |
| **v0.2.5** (`EPOCHS=300`) | **279** | 충분히 감쇠 | **2.63** |

- 일찍 멈추면 **감쇠가 효과를 내기 전에** 끝나요 → 나쁜 값.
- 끝까지 가면 학습률이 낮아진 구간에서 곱게 수렴해요 → 좋은 값.

> ✅ **v0.2.5 에서 실제로 고쳤어요** — `EPOCHS` 를 감쇠가 예산 안에서 끝나는 **300** 으로 낮추니
> epoch 279 까지 깊게 학습하고 멈춰, 검증 PPL 이 **2.96 → 2.63** 으로 내려갔습니다.

> 💡 **교훈: 하이퍼파라미터 하나가 두 역할을 겸하면, 한쪽을 못 건드려요.**
> `EPOCHS` 를 늘렸더니 **오히려 덜 학습하고 끝나는** 일이 생깁니다. 상한과 스케줄 기간은
> 원래 별개 개념이니, 실전에서는 분리하는 게 맞아요 (`T_max` 를 따로 두는 식).

## 도구별 몫을 재보면 — `1.train/sweep.py`

v0.2.2 → v0.2.3 은 도구 3개와 `EMBED` 가 **함께** 바뀐 비교라, 그것만으로는 원인을 말할 수 없어요.
그래서 `E=H=128 · N=3` 을 고정한 채 **한 번에 하나만** 켜 봤습니다.

```bash
python3 1.train/sweep.py            # 5개 전부 (MPS 로 20~40분)
python3 1.train/sweep.py --runs all none
```

| 실험 | dropout | tying | schedule | 검증PPL | 파라미터 | 기준선 대비 |
|---|---|---|---|---|---|---|
| none | — | — | — | 2.8748 | 281,865 | (기준) |
| dropout | 0.1 | — | — | 2.9081 | 281,865 | +0.033 |
| tying | — | ✅ | — | 2.9253 | **166,025** | +0.051 |
| cosine | — | — | ✅ | 2.8716 | 281,865 | **−0.003** |
| all | 0.1 | ✅ | ✅ | 2.9376 | **166,025** | +0.063 |

**읽는 법 — 이 표로 도구의 우열을 말하면 안 돼요**

- 가장 큰 도구 효과가 **0.063** 입니다.
- 그런데 도구를 **하나도 안 건드리고** `EPOCHS` 만 1,500 → 300 으로 바꾸면 **0.33** 이 개선돼요
  (2.96 → 2.63, 위 v0.2.5). **설정 하나가 도구 전부보다 5배 크게 작용**합니다.
- 그러니 "dropout 은 무효과", "tying 은 손해" 같은 **결론을 내리면 안 돼요** —
  스케줄이 정리되지 않은 상태에서 잰 값이라, 도구의 몫이 그 아래 묻혀 있습니다.
- 유일하게 흔들리지 않는 사실: **weight tying 은 파라미터를 41% 줄인다**(281,865 → 166,025).
  이건 측정이 아니라 구조에서 나오는 값이에요.

> 💡 **교훈: 측정하려면 신호가 잡음보다 커야 해요.** 도구를 제대로 비교하려면 먼저
> **스케줄을 제대로 맞춰 편차부터 줄여야** 합니다 — v0.2.5 가 그걸 해요.

## 실행

> ⚠️ **PyTorch** 필요: `pip install torch`

```bash
python3 1.train/train.py     # 학습 → 0.model/model.pt (+ vocab.json)
python3 2.test/test.py       # 학습/검증 PPL + 격차 + 이어쓰기 예시
python3 1.train/sweep.py     # 도구별 몫 실험 (모델을 저장하지 않아요)
```

- `DROPOUT` 을 0.0 ↔ 0.3, `LR_SCHEDULE` 을 `cosine` ↔ `plateau` 로 바꿔 비교해 보세요.
- 단, **한 번에 하나만** 바꿔야 원인을 말할 수 있어요.

## 임베딩 시대 진행 (v0.2.x)

| 버전 | 한 걸음 | 검증 PPL |
|---|---|---|
| v0.2.0 | 임베딩 도입 | 2.65 |
| v0.2.1 | 문맥 확장 (`block_size`) | 2.74 |
| v0.2.2 | 초기화·정규화 | 2.84 |
| **v0.2.3 ← 현재** | 일반화·튜닝 (dropout · tying · LR 스케줄) | 2.96 |
| v0.2.4 | 임베딩 시각화 (PCA · t-SNE · 최근접이웃) | 2.97 |
| v0.2.5 | 3자 비교 (캡스톤) · **완성** | 2.63 |
